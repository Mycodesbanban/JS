<h1>Suas notas</h1>
<table class="table">
    {{!-- cabecalho --}}
     <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Titulo:</th>
            <th scope="col">Ações:</th>
        </tr>
     </thead>  
      {{!--Corpo  --}}
      <tbody>
        {{#each notes}} 
        <tr>
            <th scope="row">{{@index}}</th>
            <td class="title-column"><a href="notes/{{this._id}}">{{this.title}}</a></td>
            <td class="actions-column"><a href="notes/edit/{{this._id}}" class="btn btn-primary">editar</a>
            <form action="notes/delete/" method="POST">
            <input type="hidden" name="id" value="{{this._id}}">
            <input type="submit" class="btn btn-danger" , value="remover">
            </form>
            </td>
        </tr>  
        {{else}}
        <p>Não ha notas no sistema ainda, por favor <a href="notes">crie uma nota</a>ou tente entrar novamente mais tarde</p>

        {{/each}}
      </tbody>
</table>